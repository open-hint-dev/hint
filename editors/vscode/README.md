# HINT for VS Code

Associates `.hint` with a Markdown-based HINT language, highlights vocabulary keywords, ids, and includes, completes the installed vocabulary, shows keyword descriptions on hover, opens the governing context for the current file, and shows vocabulary near-misses reported by `hint lint` after save.

The extension remains usable without the CLI: Markdown editing and the bundled keyword completions continue to work.

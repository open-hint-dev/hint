# lang

JSON (RFC 8259 / TSConfig schema)

# res isomorphicTsConfig

Extends {baseTsConfig} with no additional type overrides.
Use for packages that must compile cleanly in both browser and Node.js environments without runtime-specific globals leaking in.

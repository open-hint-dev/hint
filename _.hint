# lang

TypeScript (Node.js v20+ / ESM)

# deps

Use Node.js built-in modules as required by the specification. Do not install external parsers or heavy dependencies.

# build

- tsc for compilation
- makefile for better development and maintenance experience.
